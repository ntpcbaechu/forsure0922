🌐 index.html (前台總控與 Tab 主選單)
│
├── 📁 透過 iframe 切換 6 大功能頁面：
│
│   ├── 1️⃣ index/index_progress.html (🔥 開團與進度)
│   │   ├── 📂 讀取 DB: orders(Firebase集合)
│   │   ├── 📂 讀取 DB: photo_progress(Firebase集合)
│   │   ├── 📂 讀取 DB: event_settings(Firebase集合)
│   │   ├── 📂 寫入 DB: orders(Firebase集合)
│   │   └── 📂 寫入 DB: photo_progress(Firebase集合)
│   │
│   ├── 2️⃣ index/index_notice.html (📌 注意事項與客服小工具)
│   │   └── 📂 讀取 JSON: ../json/bank_info.json(本機檔案)
│   │
│   ├── 3️⃣ index/index_payment.html (💰 匯款表單)
│   │   ├── 📂 讀取 DB: orders(Firebase集合)
│   │   ├── 📂 讀取 DB: user_credits(Firebase集合)
│   │   ├── 📂 讀取 DB: user_deposits(Firebase集合)
│   │   ├── 📂 寫入 DB: orders(Firebase集合)
│   │   ├── 📂 寫入 DB: payments(Firebase集合)
│   │   ├── 📂 寫入 DB: user_deposits(Firebase集合)
│   │   ├── 📂 讀取 JSON: ../json/users.json(本機檔案)
│   │   ├── 📂 讀取 JSON: ../json/members.json(本機檔案)
│   │   ├── 📂 讀取 JSON: ../json/banks.json(本機檔案)
│   │   └── 📂 讀取 JSON: ../json/bank_info.json(本機檔案)
│   │
│   ├── 4️⃣ index/index_records.html (🔍 登記與對帳進度查詢)
│   │   ├── 📂 讀取 DB: orders(Firebase集合)
│   │   ├── 📂 讀取 DB: event_settings(Firebase集合)
│   │   └── 📂 讀取 JSON: ../json/members.json(本機檔案)
│   │
│   ├── 5️⃣ index/index_deposit_app.html (💳 帳戶儲值與退款中心)
│   │   ├── 📂 讀取 DB: user_deposits(Firebase集合)
│   │   ├── 📂 讀取 DB: user_credits(Firebase集合)
│   │   ├── 📂 讀取 DB: refund_requests(Firebase集合)
│   │   ├── 📂 讀取 DB: orders(Firebase集合)
│   │   ├── 📂 讀取 DB: payments(Firebase集合)
│   │   ├── 📂 寫入 DB: user_deposits(Firebase集合)
│   │   ├── 📂 寫入 DB: refund_requests(Firebase集合)
│   │   └── 📂 讀取 JSON: ../json/users.json(本機檔案)
│   │
│   └── 6️⃣ index/index_lottery.html (🎉 抽獎結果公告)
│       └── 📂 讀取 DB: lottery_events(Firebase集合)

---

🌐 admin.html (後台總控 / 各獨立管理頁面進入點)
│
├── 📄 admin_deposit.html (💳 預存儲值與抵用金管理)
│   ├── 📂 讀取 DB: user_deposits(Firebase集合)
│   ├── 📂 讀取 DB: user_credits(Firebase集合)
│   ├── 📂 讀取 DB: refund_requests(Firebase集合)
│   ├── 📂 讀取 DB: orders(Firebase集合)
│   ├── 📂 寫入 DB: user_deposits(Firebase集合)
│   ├── 📂 寫入 DB: user_credits(Firebase集合)
│   ├── 📂 寫入 DB: refund_requests(Firebase集合)
│   └── 📂 讀取 JSON: ../json/users.json(本機檔案)
│
├── 📄 admin_progress.html (🔥 發佈與更新發圖進度)
│   ├── 📂 讀取 DB: event_settings(Firebase集合)
│   ├── 📂 讀取 DB: photo_progress(Firebase集合)
│   ├── 📂 讀取 DB: orders(Firebase集合)
│   ├── 📂 寫入 DB: photo_progress(Firebase集合)
│   ├── 📂 寫入 DB: orders(Firebase集合)
│   └── 📂 讀取 JSON: ../json/members.json(本機檔案)
│
├── 📄 admin_orders.html (📄 團員登記與管理總覽專區)
│   ├── 📄 內嵌 iframe 1: orders/orders_form.html (📝 團員登記填單)
│   │   ├── 📂 讀取 DB: event_settings(Firebase集合)
│   │   ├── 📂 寫入 DB: orders(Firebase集合)
│   │   ├── 📂 讀取 JSON: ../json/users.json(本機檔案)
│   │   └── 📂 讀取 JSON: ../json/members.json(本機檔案)
│   │
│   └── 📄 內嵌 iframe 2: orders/orders_manage.html (📋 訂單管理與一鍵發圖)
│       ├── 📂 讀取 DB: orders(Firebase集合)
│       ├── 📂 寫入 DB: orders(Firebase集合)
│       ├── 📂 讀取 JSON: ../json/users.json(本機檔案)
│       └── 📂 讀取 JSON: ../json/members.json(本機檔案)
│
├── 📄 admin_lottery.html (🎁 抽獎活動管理與結果發佈)
│   ├── 📂 讀取 DB: event_settings(Firebase集合)
│   ├── 📂 讀取 DB: orders(Firebase集合)
│   ├── 📂 讀取 DB: lottery_events(Firebase集合)
│   ├── 📂 寫入 DB: lottery_events(Firebase集合)
│   └── 📂 讀取 JSON: ../json/members.json(本機檔案)
│
├── 📄 admin_events.html (⚙️ 活動單價與成本設定)
│   ├── 📂 讀取 DB: event_settings(Firebase集合)
│   ├── 📂 讀取 DB: orders(Firebase集合)
│   ├── 📂 讀取 DB: photo_progress(Firebase集合)
│   ├── 📂 寫入 DB: event_settings(Firebase集合)
│   ├── 📂 寫入 DB: photo_progress(Firebase集合)
│   ├── 📂 讀取 JSON: ../json/members.json(本機檔案)
│   └── 📂 讀取 JSON: ../json/sellers.json(本機檔案)
│
├── 📄 admin_payments.html (💰 匯款對帳審核總控台)
│   ├── 📄 內嵌 iframe 切換以下 4 個子頁面 (共同讀寫 DB)：
│   │   ├── 📄 payment/payment_pending.html (🟡 未對帳審核)
│   │   ├── 📄 payment/payment_ok.html (🟢 已對帳審核)
│   │   ├── 📄 payment/payment_error.html (🔴 帳有問題審核)
│   │   └── 📄 payment/payment_photosent.html (🔵 已發圖專區)
│       ├── 📂 讀取 DB: payments(Firebase集合)
│       ├── 📂 讀取 DB: orders(Firebase集合)
│       ├── 📂 讀取 DB: user_credits(Firebase集合)
│       ├── 📂 讀取 DB: user_deposits(Firebase集合)
│       ├── 📂 寫入 DB: payments(Firebase集合)
│       ├── 📂 寫入 DB: orders(Firebase集合)
│       ├── 📂 寫入 DB: user_credits(Firebase集合)
│       └── 📂 寫入 DB: user_deposits(Firebase集合)
│
└── 📄 admin_ctbc_bank.html (🏦 中信存款交易明細對帳系統)
    ├── 📂 讀取 JS: ctbc_data.js(本機檔案)
    ├── 📂 讀取 JSON: ../json/users.json(本機檔案)
    └── 📂 讀取 JSON: ../json/members.json(本機檔案)
